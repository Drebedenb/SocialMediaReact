import React from 'react';
import styles from "./DialogMessages.module.css"

const ChooseDialogMessage = () => {
    return (
        <strong className={styles.chooseDialogMessage}>
            Choose your dialog
        </strong>
    );
};

export default ChooseDialogMessage;